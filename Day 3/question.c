/*
 * Sample question to test understanding of if...else
 */

#include <stdio.h>

int main () {

	/* looks scary
	 * printf("%d", printf("geeks"));
	 */

	/* == refers to equality
	 * != refers to not equality
	 */

	if (printf("geeks") != 5) {

	}
	else {
		printf("geeksforgeeks");
	}

	return 0;
}